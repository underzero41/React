import React from 'react';

const ExampleComponent = ({ data }) => {
    return (
        <div>
            <h1>Данные:</h1>
            <p>{data}</p>
        </div>
    );
};

export default ExampleComponent;
